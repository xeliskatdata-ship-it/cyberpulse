# CyberPulse — dbt project configuration
# À compléter en Sprint 3

name: 'cyberpulse'
version: '1.0.0'
config-version: 2

profile: 'cyberpulse'

model-paths: ["models"]
analysis-paths: ["analyses"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]
snapshot-paths: ["snapshots"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  cyberpulse:
    staging:
      +materialized: view
    mart:
      +materialized: table
